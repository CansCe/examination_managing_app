# Chat Service - MongoDB Configuration
# Copy this file to .env and fill in your MongoDB credentials

# MongoDB Connection URI (same as backend-api)
# Format: mongodb://username:password@host:port/database
# Example: mongodb://localhost:27017/exam_management
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017/exam_management

# MongoDB Database Name (optional, defaults to 'exam_management')
MONGODB_DB=exam_management

# Optional: Default Admin ID (MongoDB ObjectId format)
# DEFAULT_ADMIN_ID=507f1f77bcf86cd799439011

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated origins)
# Include your server domains for production deployment
# For production, add: https://exam-app-api.duckdns.org,https://backend-chat.duckdns.org,http://exam-app-api.duckdns.org,http://backend-chat.duckdns.org
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:3001,https://exam-app-api.duckdns.org,https://backend-chat.duckdns.org,http://exam-app-api.duckdns.org,http://backend-chat.duckdns.org

# Optional: Shutdown token for development (POST /shutdown)
# SHUTDOWN_TOKEN=your-secret-token
