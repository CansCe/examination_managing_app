# Chat Service - backend-chat.duckdns.org
# Place this file in /etc/nginx/sites-available/backend-chat.duckdns.org
# Socket.io WebSocket support included

server {
    listen 80;
    server_name backend-chat.duckdns.org;

    # Logging
    access_log /var/log/nginx/backend-chat-access.log;
    error_log /var/log/nginx/backend-chat-error.log;

    # Proxy to Docker container (Socket.io support)
    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        
        # WebSocket support for Socket.io
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Socket.io specific settings
        proxy_buffering off;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts for long-lived connections (7 days for Socket.io)
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}

# After SSL certificate is installed, Certbot will add:
# - listen 443 ssl;
# - ssl_certificate and ssl_certificate_key directives
# - Redirect from HTTP to HTTPS

